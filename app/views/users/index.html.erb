<h1>All Users</h1>
<%= link_to "Projects", projects_path %> | <%= link_to "Users", users_path %> | <%= link_to "New Project", new_project_path %> | <%= link_to "New User", new_user_path %> 

<p> ------------------------------------------------- </p>

<% @users.each do |user| %>
  <li> 
    <p> <%= link_to user.name, user_path(user) %> <%= link_to user.last_name, user_path(user) %> 
    <font size="1"><%= link_to "Edit", edit_user_path(user) %> <%= button_to('Delete', user_path(user), {:onclick => "return confirm('Are you sure')", method: :delete }) %>
      
 </font>

   </font>
    </p> 
  </li>
<% end %>