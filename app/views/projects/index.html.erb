<h1>All Projects</h1>

<p><a href="/projects/new" class="btn btn-primary">New Project</a> </p>
</p>

</br>

 <p> <% @project.each do |project| %>
 <%= link_to project.project_name, project_path(project.id) %> 
  <font size="1"><%= link_to "Edit", edit_project_path(project) %> <%= button_to 'Delete', project_path(project), method: :delete %>
 </font>
 </p>
<% end %>
